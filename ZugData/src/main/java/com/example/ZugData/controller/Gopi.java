package com.example.ZugData.controller;

import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class Gopi {

//api/v1/{userId}?value1=20
    ("api/v1")
    @GetMapping(@id={userid},value=value1)
    ObjectOne getEmployee(userid,value1)
    {
// get object from service
        //get userid and value send it
        Employee = service.getEmployee(userid,value1);



    }







}
